name: DevSecOps ZAP Scan

on:
  pull_request:
    branches: [ "main" ]

jobs:
  security-dast-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Install Dependencies
        run: pnpm install

      - name: Build and Start Application in Background
        run: |
          pnpm run build
          pnpm run start & 
          sleep 10 

      # FIX: Grants the ZAP Docker container permission to write reports to the folder
      - name: Fix Directory Permissions 
        run: chmod -R 777 .

      - name: OWASP ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: 'http://localhost:3000' 
          fail_action: true 
          # Removed the rules_file_name so it defaults to standard rules
          cmd_options: '-a'